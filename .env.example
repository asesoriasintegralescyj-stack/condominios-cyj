# ===========================================
# VARIABLES DE ENTORNO - SISTEMA CYJ CONDOMINIOS
# ===========================================
# Copia este archivo como .env.local para desarrollo local
# O configura estas variables en Vercel para producción

# -------------------------------------------
# BASE DE DATOS (PostgreSQL en Neon)
# -------------------------------------------
# Obtén esta URL desde el dashboard de Neon:
# https://console.neon.tech/app/projects -> tu proyecto -> Connection Details
# Formato: postgresql://usuario:password@host/neondb?sslmode=require
DATABASE_URL="postgresql://usuario:password@ep-tu-host.neon.tech/neondb?sslmode=require"

# -------------------------------------------
# NEXTAUTH (Autenticación)
# -------------------------------------------
# URL de tu aplicación (cambiar en producción)
NEXTAUTH_URL="http://localhost:3000"
# En producción: https://tu-dominio.vercel.app

# Secreto para JWT (generar con: openssl rand -base64 32)
NEXTAUTH_SECRET="generar-con-openssl-rand-base64-32"

# -------------------------------------------
# ENCRIPTACIÓN DE DATOS SENSIBLES (OBLIGATORIO EN PRODUCCIÓN)
# -------------------------------------------
# Clave para encriptar teléfono y dirección de usuarios (AES-256-GCM)
# Generar con: openssl rand -base64 32
# ⚠️ OBLIGATORIA en producción — si no está configurada, la app fallará al iniciar
ENCRYPTION_KEY="generar-con-openssl-rand-base64-32"

# Salt para derivar la clave (opcional, hay un default)
# ENCRYPTION_SALT="condominio-cyj-prod-salt"

# -------------------------------------------
# ADMIN INICIAL (Solo para setup inicial)
# -------------------------------------------
# Email y password del admin creado por /api/setup o /api/auth/init-admin
# Solo funciona en desarrollo (los endpoints están bloqueados en producción)
# INITIAL_ADMIN_EMAIL="admin@cyj.cl"
# INITIAL_ADMIN_PASSWORD="password-seguro-min-8-chars"

# -------------------------------------------
# OPCIONAL: APIs Externas
# -------------------------------------------
# OpenAI API (para funcionalidades de IA)
# OPENAI_API_KEY="sk-xxxxx"

# Email SMTP (para notificaciones)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="tu-email@gmail.com"
# SMTP_PASSWORD="tu-app-password"
